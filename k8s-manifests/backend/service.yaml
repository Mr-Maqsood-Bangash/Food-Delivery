apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: frontend
spec:
  type: ClusterIP   # Internal only
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 4000        # Service port
      targetPort: 4000  # Container por

