apiVersion: v2
name: three-tier-app
description: >
  Helm chart for a three-tier MERN application (React frontend, Node/Express
  backend, MongoDB StatefulSet) deployed on a Kind Kubernetes cluster.
type: application
version: 0.1.0
appVersion: "1.0.0"
